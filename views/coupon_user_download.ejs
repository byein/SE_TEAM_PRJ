<!DOCTYPE html>
<html lang="kr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/coupon_user.css">
    <title>쿠폰 다운로드</title>

</head>
<body>

    <div class="download">
        <h2>쿠폰 다운받기</h2>
    </div>

    <br>

    <table>
        <thead>
            <th>쿠폰명</th>
            <th>카테고리</th>
            <th>할인율</th>
            <th>할인한도</th>
            <th>발급</th>
        </thead>
        <% if(!coupon) { } else { %>

        <tbody>
        <% for (let i=0; i<coupon.length; i++) { %>
                    <tr>
                        <% if (coupon[i].cpType == 0) { %>
                <td><%= coupon[i].cpName %></td>
                <td><%= coupon[i].cpCategory %></td>
                <td><%= coupon[i].cpCategorySale %>%</td>
                <td><%= coupon[i].cpCategoryMax %>원</td>
                <td><a href="/coupon_download/<%= coupon[i].cpIdx %>" >쿠폰발급</a></td>
                        <% }else { %>
                        <% } %>
                    </tr>
                    <% } %>
                </tbody>
                <% } %>

                <thead>
                    <th>쿠폰명</th>
                    <th>최소주문금액</th>
                    <th>할인금액</th>
                    <th>발급</th>
                </thead>
                <% if(!coupon) { } else { %>

                <tbody>
                        <% for (let i=0; i<coupon.length; i++) { %>
                    <tr>
                        <% if (coupon[i].cpType == 1) { %>
                <td><%= coupon[i].cpName %></td>
                <td><%= coupon[i].cpMiniPrice %>원</td>
                <td><%= coupon[i].cpPriceSale %>원</td>
                <td><a href="/coupon_download/<%= coupon[i].cpIdx %>"> 쿠폰발급</a></td>
                        <% } else { %>
                        <% } %>
                    </tr>
                    <% } %>
                </tbody>
                <% } %>

            </table>
        </div>

</body>
</html>